/** Full citation metadata remains in browser storage, not repeated in every model request. */
export function corpusLedgerContext(): string {
	return "Source citation metadata is retained locally. This notice is not evidence. Recent results and conversation summaries contain source handles; use conversation_history to recover older exact handles and corpus_read to reread source text before relying on procedural details.";
}
