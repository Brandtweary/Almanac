# TLS reverse proxy for the application gateway.
# Local loopback installation does not require a public domain.
# Browsers require HTTPS for microphone access on non-loopback hosts.
your-domain.example {
    redir /almanac /almanac/
    handle_path /almanac/* {
        reverse_proxy 127.0.0.1:8790
    }
}
