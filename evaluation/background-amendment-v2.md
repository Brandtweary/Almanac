# Corrected background fixture amendment

Version 2 corrects two audit fixtures without modifying the original frozen campaign, receipts or reviews. `loadCorrectedBackgroundSanityCases()` returns `background.voice-and-stale-description-v2` and `background.glossary-correction-and-matching-v2`. The historical loader and its three original case IDs remain available.

Both corrected cases retain their synthetic user text, initial glossary, prior evidence and rubric clauses, but include actual retrieval context. The adapter runs the production `RecallPool` against the user message, checks the resulting labels against the fixture, commits a synthetic prior-chat delivery, and formats its record through production `formatTranscript`. The admitted `<memory>` content remains retrieval evidence, never a user quotation. Prior-chat admission uses a disclosed conservative UTF-8 byte count; it is not a native model-token measurement. These remain isolated audit probes, not integrated audit→memory→summary tests.

The shared `audit_handoff` description states its conditional fields: spelling requires transcribed/spoken/utteranceId; stale-description requires termId and a nonempty reason. Shared execution validates them with explicit errors before publication. The evaluator also matches the production optional-property guard instead of raising an undefined-property exception. This clarifies an existing contract; audit authority is unchanged.

The installed `word-list` dictionary contains `snacking` and excludes `snocking`; production `validateAutoReplace` accepts the pair. The original recognition/logging rubric is not retroactively strengthened into a requirement to execute an automatic rule. Empty phonetic candidates are likewise not proof of no error: candidate targets come from the stored vocabulary and accepted observations, not every dictionary word.

Run the two corrected cases once for every original candidate: 20 trajectories, 16 completions and 180 seconds each, unchanged sampling and per-candidate cumulative stage limits. Freeze corrected source, case bytes, provider pins and rubrics after review. Wait for the main campaign and routing replay to settle; use their cumulative ledger and owner with distinct amendment reservation IDs. The approved routing amendment applies where relevant. There is no independent spending allocation and no model-based selection of which candidates receive corrected fixtures.

For original results, preserve literal frozen judgments and tool traces while qualifying the affected retrieval-scope clauses and handoff-contract error. Do not count direct edits to un-retrieved descriptions as evidence of clean audit-policy compliance, or scoped abstention as an unqualified defect. Other observed spelling, provenance and tool-outcome facts remain reportable. Version 2 results form a separate comparison; they do not overwrite measured answers or erase unrelated failures.
