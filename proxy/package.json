{
  "name": "almanac-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local inference and corpus gateway",
  "scripts": {
    "start": "bun run server.ts",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "4.12.26"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.7.3"
  }
}
